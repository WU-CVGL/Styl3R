defaults:
    - _self_
    - task: cls
    - model: ssg
    - task_model: ${task}-${model}

hydra:
  run:
    dir: outputs

gpus:
    - 0

optimizer: ???

task_model: ???

model: ???

distrib_backend: dp
